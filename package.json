{
  "name": "wortheyflow",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "start": "cd server && node server.js",
    "install": "cd server && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}
